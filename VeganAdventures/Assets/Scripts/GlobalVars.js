
static var score : float = 0;
static var veggiesLeft : int = 10;
static var lives : int = 5;
static var hitDelay : float = 0;

//dirty fix because 180 * Time.deltaTime = 3, so multiply all seconds value by 60 to get true time seconds
static var timer : int = 241 * 60;

function Update () {
}